section.banner
    .container
        .section-title
            .title
                .romb.romb-left
                h2.title__text amazing &#160;
                    span.accent-color features
                .romb.romb-right
            .title__subtext With unlimited features that we offer, we promise it’s possible to make everything that was impossible for you !
        ul.previewList
            each item in bannerList
                li.item
                    .icon.accent-color(class=item.iconClass)
                    .description
                        .name-service=item.name
                        .text=item.text
