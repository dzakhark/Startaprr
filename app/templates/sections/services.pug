section.services
    .container
        ul.list__services
            each service in services
                li.item
                    .label
                        .icon(class=service.iconClass)
                        .top-section
                        .bottom-section
                    .description
                        .name=service.name
                        .short-inf=service.shortInf
                        .text=service.text